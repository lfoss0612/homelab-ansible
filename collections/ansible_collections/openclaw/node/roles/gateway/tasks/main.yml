---
# -----------------------------------------------------------------------------
# Gateway deployment orchestration (FIXED ORDER)
# -----------------------------------------------------------------------------

# 1. Install/update release contents
- import_tasks: install.yml

# 2. Build inside the release directory
- import_tasks: build.yml

# 3. Package runtime artifact for nodes
- import_tasks: package-artifact.yml

# 4. Flip current -> release symlink LAST (atomic deploy)
- import_tasks: symlink.yml
