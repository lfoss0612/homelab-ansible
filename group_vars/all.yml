# -----------------------
# OpenClaw global config
# -----------------------

openclaw_install_dir: "/opt/openclaw"

# Safety: deterministic installs unless tag changes
openclaw_force_reinstall: false
